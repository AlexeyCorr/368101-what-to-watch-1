export const history = {
  action: `PUSH`,
  block: jest.fn(),
  createHref: jest.fn(),
  go: jest.fn(),
  goBack: jest.fn(),
  goForward: jest.fn(),
  length: 2,
  listen: jest.fn(),
  location: {pathname: `/login`, search: ``, hash: ``, state: undefined, key: `rja7sx`},
  push: jest.fn(),
  replace: jest.fn(),
};
